pip install pandas
pip install fonttools
pip install Pillow
pip install pypdf
pip install requests
pip install borb

Make sure no text in your database uses a single quote character (‘). Replace this with an apostrope ('). It throws off the reading of the csv.

Add all named image files to the /originals folder. Make sure the names for these files match the names you've indicated for the front and back images in the database